const myName = 'Vlad'
const age = 29
const isMarried = true
const job = null
const currentLocation = undefined
const favorites = {
  game: 'Skyrim',
  music: 'RetroWave',
  movie: 'Ready Player One',
  car: 'Lamborghini Huracán',
  number: 21,
}
const id = Symbol('id')
const bigNumber = 21n

console.log('myName', myName, typeof myName)
console.log('age', age, typeof age)
console.log('isMarried', isMarried, typeof isMarried)
console.log('job', job, typeof job)
console.log('currentLocation', currentLocation, typeof currentLocation)
console.log('favorites', favorites, typeof favorites)
console.log('id', id, typeof id)
console.log('bigNumber', bigNumber, typeof bigNumber)
